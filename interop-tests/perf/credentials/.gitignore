# secrets
kanon.env

# captured corpora / benchmark artifacts (regenerated by `idiom-perf capture`)
*.ndjson

# Credo 0.7 bench project deps (npm install regenerates)
credo-bench/node_modules/

# transient dbs
*.db
